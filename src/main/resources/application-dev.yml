# ===== DEV PROFILE =====

logging:
  level:
    com.zaxxer.hikari: DEBUG
    org.springframework.jdbc: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/woodexcess
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

security:
  refresh-token:
    max-devices: 5
    rotation-enabled: true
    validate-context: true
    reuse-detection: true
  login:
    rate-limit:
      capacity: 100
      refill-tokens: 100
      refill-minutes: 1

# AWS S3 Configuration (DEV)
aws:
  s3:
    access-key: ${AWS_ACCESS_KEY_ID}
    secret-key: ${AWS_SECRET_ACCESS_KEY}
    region: us-east-1
    bucket-name: woodexcess-listings
    # endpoint: http://localhost:4566  # Descomentar para LocalStack
